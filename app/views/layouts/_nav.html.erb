<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <li class="nav-item">
      <%= link_to 'Transact', root_path, class: "navbar-brand #{'active' if request.path == '/' }" %>
    </li>
    <% if current_user %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item ">
            <%= link_to 'My Spenses', entities_path, class: "nav-link #{'active' if request.path == '/entities' }" %>
          </li>
          <li class="nav-item">
            <%= link_to 'My Budgets', root_path, class: "nav-link #{'active' if request.path == '/' }" %>
          </li>
        </ul>
        <ul class='navbar-nav ml-auto'>
          <li class='nav-item user-name'>
            <%= fa_icon 'user' %> <%= current_user.name %>
          </li>
          <li class='nav-item'>
            <%= button_to 'Logout', destroy_user_session_path, class: "btn btn-danger logout-button", method: :delete %>
          </li>
        </ul>
      </div>
    <% else %>
      <%= link_to 'Register', new_user_registration_path, class:"nav-item " %> or <%= link_to 'Sign in', new_user_session_path, class:"nav-item " %>
    <% end %>
  </div>
</nav>
